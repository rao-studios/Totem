import Foundation

/// Gita stub for Totem, this handles the actual contribution calculation of
/// each result and its impact on a LLM response. The search is part 1 (Totem), the
/// personalized chat response with royalties is part 2 available in the full Database
/// implementation.
enum Gita {
    typealias Credits = Double
}
